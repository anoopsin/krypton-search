# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /search/:query                           controllers.Search.search(query: String, bestResultsOnly: Boolean ?= false)
